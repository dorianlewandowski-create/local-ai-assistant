[
  {
    "name": "file_list",
    "description": "List files in a directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The directory path."
        }
      },
      "required": [
        "path"
      ]
    },
    "agent": true
  },
  {
    "name": "file_find",
    "description": "Find files by pattern or extension.",
    "parameters": {
      "type": "object",
      "properties": {
        "dir": {
          "type": "string",
          "description": "The directory."
        },
        "name": {
          "type": "string",
          "description": "A name pattern."
        },
        "ext": {
          "type": "string",
          "description": "Extensions."
        }
      },
      "required": [
        "dir"
      ]
    },
    "agent": true
  },
  {
    "name": "file_move",
    "description": "Move a file or directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The source."
        },
        "destination": {
          "type": "string",
          "description": "The destination."
        }
      },
      "required": [
        "source",
        "destination"
      ]
    },
    "agent": true
  },
  {
    "name": "file_rename",
    "description": "Rename a file or directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The source."
        },
        "destination": {
          "type": "string",
          "description": "The new name/path."
        }
      },
      "required": [
        "source",
        "destination"
      ]
    },
    "agent": true
  },
  {
    "name": "file_delete",
    "description": "Delete a file or directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The path to delete."
        }
      },
      "required": [
        "path"
      ]
    },
    "agent": true
  },
  {
    "name": "file_classify",
    "description": "Classify files for organization.",
    "parameters": {
      "type": "object",
      "properties": {
        "files": {
          "type": "string",
          "description": "Comma-separated list of files."
        }
      },
      "required": [
        "files"
      ]
    },
    "agent": true
  },
  {
    "name": "fs_mkdir",
    "description": "Create a directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The path."
        }
      },
      "required": [
        "path"
      ]
    },
    "agent": true
  },
  {
    "name": "consult_agent",
    "description": "Consult another specialist agent for an opinion or information.",
    "parameters": {
      "type": "object",
      "properties": {
        "agent_name": {
          "type": "string",
          "description": "The name of the agent to consult (pa, wa, fm, coder)."
        },
        "query": {
          "type": "string",
          "description": "The specific question or task for the specialist."
        }
      },
      "required": [
        "agent_name",
        "query"
      ]
    },
    "agent": true
  }
]
