/**
 * Installation root (repo / release layout) for cwd-independent paths.
 * Implemented in `@apex/macos-node` (native bridge + layout markers).
 */
export { resolveApexInstallRoot } from '@apex/macos-node'
